package ch07.sec06.package2;

public class C {
    public void method() {
        // A a = new A();
        // a.field = "value";
        // a.method();
        // C 클래스는 A 클래스와 다른 패키지에 있기 때문에
        // A 에 접근할 수 없다.
    }
}
